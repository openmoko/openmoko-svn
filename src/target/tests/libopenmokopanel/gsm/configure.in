#                                               -*- Autoconf -*-
# Process this file with autoconf to produce a configure script.

PACKAGE=gsmpanel

AC_PREREQ(2.59)

m4_define([gsmpanel_version],[0.0.1])

AC_INIT(libfilemutex, [gsmpanel_version], [http://gsmpanel.org])
#AC_CONFIG_SRCDIR([mutextest.c])
#AC_INIT(mutextest.c)
AM_INIT_AUTOMAKE(gsmpanel, [gsmpanel_version])


# Checks for programs.
AC_PROG_CC
AC_PROG_INSTALL
#AC_ARG_PROGRAM
AM_PROG_LIBTOOL

# Checks for libraries.
PKG_CHECK_MODULES(GTK, gtk+-2.0 gdk-2.0,,exit)
AC_SUBST(GTK_LIBS)
AC_SUBST(GTK_CFLAGS)
PKG_CHECK_MODULES(MB, libmb,,exit)
AC_SUBST(MB_LIBS)
AC_SUBST(MB_CFLAGS)

# Checks for header files.
#AC_HEADER_STDC
#AC_CHECK_HEADERS([fcntl.h stdlib.h string.h unistd.h])

# Checks for typedefs, structures, and compiler characteristics.

# Checks for library functions.
AC_CHECK_FUNCS([memset])

AC_CONFIG_FILES([Makefile])
AC_OUTPUT
